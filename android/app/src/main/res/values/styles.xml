<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Toolbar Style -->
    <style name="CustomToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/toolbarBackground</item>
        <item name="android:theme">@style/CustomToolbarTheme</item>
        <item name="popupTheme">@style/CustomPopupTheme</item>
    </style>

    <style name="CustomToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="CustomPopupTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorControlNormal">@color/black</item>
    </style>

    <!-- Logout Button Style -->
    <style name="LogoutButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/logoutButtonBackground</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>
</resources>
